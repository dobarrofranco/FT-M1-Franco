// call = hace un string AHORA CON SPREAD OPERATOR (...) SE USA CALL
// apply = es para arrays EN DESUSO
// bind = toma una función para usarla donde lo necesites.
            // funcion.atar (objeto) = funcion.atar (objeto, parámetros, parámetros...) 
            // Es decir llama a la función que queremos y le pasamos con que objeto queremos aplicarla!
            // si le pasamos un parámetro en específico cuando usamos el bind pisamos las propiedades del objeto!
